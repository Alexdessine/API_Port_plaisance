<h1>Bienvenue sur l'application</h1>

<% if (!isAuthenticated) { %>
    <p style="color: #c0392b">
        ⚠️ Vous n'êtes pas identifié.
    </p>

    <p>
        Veuillez vous connecter pour accéder au tableau de bord.
    </p>

    <a href="/auth/login">Se connecter</a>
    <% } else { %>
        <p>
            Connecté en tant que <strong>
                <%= user.firstname %>
                    <%= user.name %>
            </strong>
        </p>
        <p>
            <small>Dernière connexion le <%= user.lastLoginAt ? new Date(user.lastLoginAt).toLocaleString('fr-FR')
                    : "Jamais" %></small>
        </p>

        <a href="/dashboard">Accéder au tableau de bord</a> <a href="/auth/logout">Se déconnecter</a>
        <% } %>