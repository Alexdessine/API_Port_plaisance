<nav>
    <a href="/dashboard">← Retour au dashboard</a>
    <a href="/api-docs" target="_blank">Documentation API</a>
    <a href="/auth/logout">Se déconnecter</a>
</nav>

<h1>Modifier une réservation</h1>

<% if (error) { %>
    <p style="color:red;">
        <%= error %>
    </p>
    <% } %>

        <% if (!reservation) { %>
            <p>Réservation introuvable.</p>
            <% } else { %>

                <form action="/dashboard/reservations/<%= reservation._id %>?_method=PATCH" method="POST">
                    <fieldset>
                        <legend>Informations générales</legend>

                        <p>
                            <label>Catway :</label><br>
                            <input type="number" value="<%= reservation.catwayNumber %>" disabled>
                        </p>

                        <p>
                            <label for="clientName">Nom du client</label><br>
                            <input type="text" id="clientName" name="clientName" value="<%= reservation.clientName %>"
                                required>
                        </p>

                        <p>
                            <label for="boatName">Nom du bateau</label><br>
                            <input type="text" id="boatName" name="boatName" value="<%= reservation.boatName %>"
                                required>
                        </p>
                    </fieldset>

                    <fieldset>
                        <legend>Période de réservation</legend>

                        <p>
                            <label for="startDate">Date de début</label><br>
                            <input type="date" id="startDate" name="startDate"
                                value="<%= reservation.startDate.toISOString().substring(0, 10) %>" required>
                        </p>

                        <p>
                            <label for="endDate">Date de fin</label><br>
                            <input type="date" id="endDate" name="endDate"
                                value="<%= reservation.endDate.toISOString().substring(0, 10) %>" required>
                        </p>
                    </fieldset>

                    <button type="submit">💾 Enregistrer les modifications</button>
                    <a href="/dashboard/show/<%= reservation._id %>">Annuler</a>
                </form>

                <% } %>